server:
  port: 8080

spring:
  application:
    name: AriMayiChallenge

  datasource:
    url: jdbc:mysql://mysql:3306/resources_management_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: user
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
